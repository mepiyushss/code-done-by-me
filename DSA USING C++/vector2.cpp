#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, i, m, x;
    cin >> n;
    vector<vector<int>> v;
    vector<int> temp;
    cin >> m;
    for (i = 0; i < m; i++)
    {
        cin >> x;
        temp.push_back(x);
    }
    v.push_back(temp);
}